#!/bin/bash
if pgrep -x ghostty >/dev/null; then
  killall -SIGUSR2 ghostty
fi

pkill -x swaybg
setsid uwsm-app -- swaybg -i "$ARCHY_PATH"/theme/background.png -m fill >/dev/null 2>&1 &

hyprctl reload >/dev/null

tmux source-file ~/.config/tmux/.tmux.conf
